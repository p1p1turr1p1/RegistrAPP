import { RegistroAsistencia } from './registro-asistencia';

describe('RegistroAsistencia', () => {
  it('should create an instance', () => {
    expect(new RegistroAsistencia()).toBeTruthy();
  });
});
